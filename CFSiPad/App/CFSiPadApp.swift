//
// CFSiPadApp.swift
// 应用入口（M0 骨架）
// 作者: ZJ
//

import SwiftUI

/// App 入口。M0 阶段仅挂载占位根视图；
/// M4 完成编组界面后替换为正式的根导航结构。
@main
struct CFSiPadApp: App {
    var body: some Scene {
        WindowGroup {
            RootPlaceholderView()
        }
    }
}
