# 安装与使用手册

适用：iPadOS 26+，Windows PC，免费 Apple ID 自签安装（个人自用）。

## 一、准备 PC 端服务器

1. 从原项目 [Releases](https://github.com/WisteFinch/Helldivers2CallForStratagemsOnPhone/releases/latest) 下载 `call-for-stratagems-server-*.exe`（或在本地仓库 `server/` 目录 `cargo run`）。
2. 运行服务器。**首次运行 Windows 防火墙会弹窗，务必勾选"专用网络"并允许**——不允许的话 iPad 永远连不上。
3. 服务器控制台会打印监听地址和连接二维码，保持窗口开着。
4. iPad 与 PC 必须在同一局域网（同一路由器 Wi-Fi）。

## 二、获取 IPA

仓库 GitHub Actions 每次 push 自动出包：仓库页 → Actions → 最新一次绿色 run → Artifacts → 下载 `CFSiPad-unsigned-ipa-*`（zip），解压得到 `CFSiPad-unsigned.ipa`。

## 三、安装到 iPad（爱思助手，主路径）

1. Windows 安装[爱思助手](https://www.i4.cn/)，USB 连接 iPad。
2. 工具箱 → IPA 签名：添加 `CFSiPad-unsigned.ipa`，选择"使用 Apple ID 签名"，登录你的（免费）Apple ID，签名后安装到设备。
3. 首次安装需要在 iPad 上：
   - 开启开发者模式：设置 → 隐私与安全性 → 开发者模式 → 开启并重启；
   - 信任证书：设置 → 通用 → VPN 与设备管理 → 信任你的 Apple ID 对应的开发者应用。

备选：[Sideloadly](https://sideloadly.io/)（本项目实测在部分环境签名报错，故推荐爱思助手）。

### 7 天续签

免费 Apple ID 证书 7 天有效，到期后 App 无法打开（数据不丢）。用爱思助手对最新 IPA 重新签名安装（覆盖安装）即可继续使用。免费账号同时最多 3 个自签 App。

## 四、首次使用流程

1. 打开 App → 右上角齿轮进设置；
2. **更新数据库**（战备数据库区 → 更新数据库），等待图标下载完成；
3. **扫描服务器二维码**（或手动填 PC 的局域网 IP 和端口 23333）→ 测试连接；
4. 首次连接 iPad 会弹**本地网络权限**，必须允许；
5. 认证提示出现时，到 PC 服务器控制台按 `Y` 确认（认证记录默认保留 3 天）；
6. 回到首页新建编组、勾选战备，进入游玩页即可滑动呼叫。

## 五、常见问题

| 现象 | 处理 |
|---|---|
| 连接一直失败 | 依次检查：同一 Wi-Fi？PC 防火墙放行了服务器？IP 填的是服务器窗口显示的地址？ |
| 首次弹本地网络权限点了"不允许" | 设置 → 隐私与安全性 → 本地网络 → 打开本 App 的开关 |
| 状态条一直"等待认证" | 到 PC 服务器控制台按 Y；被拒后需重新连接再次触发 |
| 战备图标显示问号 | 设置 → 更新数据库（图标是联网下载的） |
| App 突然打不开 | 签名过期（7 天），重新签名安装即可，数据保留 |
| 官方数据源访问失败 | 设置 → 数据源改为"自定义 URL"，指向自建镜像的 index.json |

## 六、日常玩法建议

游戏开始前：PC 起服务器 → iPad 打开 App 进入编组游玩页（保持前台亮屏，App 会自动屏幕常亮）。普通模式按提示滑动输入呼叫；紧急情况直接左右滑列表条目立即呼叫；熟练后可在设置里开简化模式（纯网格点击）。
